# Plugins

Registrations centralizadas que não cabem em `main.ts`. Atualmente vazio — Pinia, Router e Toaster são registrados diretamente em `main.ts` / `App.vue`. Adicione aqui plugins de terceiros conforme necessário (ex.: vue-query, sentry, i18n).
